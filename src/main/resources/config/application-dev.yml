#4-8:数据库配置文件,  9-22:连接池配置信息  ,  23-30:druid WebStatFilter监控配置, 31-38:druid StatViewServlet监控配置
spring:
    datasource:
        url: jdbc:mysql://rm-wz99x87gp7wo0bvp4xo.mysql.rds.aliyuncs.com:3306/shop?allowMultiQueries=true&useUnicode=true&characterEncoding=UTF-8  	
        username: root
        password: cqu2010!!
        type: com.alibaba.druid.pool.DruidDataSource
        driver-class-name: com.mysql.jdbc.Driver
        initial-size: 5
        min-idle: 5
        max-active: 20
        max-wait: 60000
        time-between-eviction-runs-millis: 60000
        min-evictable-idle-time-millis: 300000
        validation-query: SELECT 1 FROM DUAL
        test-while-idle: true
        test-on-borrow: false
        test-on-return: false
        pool-prepared-statements: false
        max-pool-prepared-statement-per-connection-size: 20
        filters: stat,wall
        connection-properties: druid.stat.mergeSql=true;druid.stat.slowSqlMillis=5000
        druid.web-stat-filter.enabled: true
        druid.web-stat-filter.url-pattern: /*
        druid.web-stat-filter.exclusions: value=*.js,*.gif,*.jpg,*.bmp,*.png,*.css,*.ico,/druid/*
        druid.web-stat-filter.session-stat-enable: true
        druid.web-stat-filter.session-stat-max-count: 10
        druid.web-stat-filter.principal-session-name:
        druid.web-stat-filter.principal-cookie-name:
        druid.web-stat-filter.profile-enable:
        druid.stat-view-servlet.enabled: true
        druid.stat-view-servlet.url-pattern: /druid/*
        druid.stat-view-servlet.reset-enable: false
        druid.stat-view-servlet.login-username: admin
        druid.stat-view-servlet.login-password: admin
        druid.stat-view-servlet.allow: 192.168.1.115,127.0.0.1
        druid.stat-view-servlet.deny: 192.168.1.115
        druid.aop-patterns: tech.reyao.shop.*.service  
        
    thymeleaf:
        cache: false
        
        